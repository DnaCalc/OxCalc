# Archived Replay Tooling Source

This directory contains historical TraceCalc runner/classifier source files retired during the W046 cleanup prelude.

The files are retained for provenance and occasional recovery. They are not active crate modules, CLI commands, or current W046 specification surfaces. Before reusing any logic from these files, distill the semantic content into an active spec, model, replay fixture, or implementation change.

Archived from `src/oxcalc-tracecalc/src/`:

1. `continuous_assurance.rs`
2. `diversity_seam.rs`
3. `formal_assurance.rs`
4. `implementation_conformance.rs`
5. `operated_assurance.rs`
6. `oxfml_seam.rs`
7. `pack_capability.rs`
8. `scale_semantic_binding.rs`
9. `stage2_replay.rs`
