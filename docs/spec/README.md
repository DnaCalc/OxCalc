# OxCalc Spec Index

This directory is the OxCalc-owned mutable spec set after bootstrap.

## Canonical spec ownership (post-bootstrap)
- `docs/spec/core-engine/*` (coordinator/core engine baseline and realization docs)
- `docs/spec/visibility/*` (visibility-priority policy details)

## Consumed mirror set
- `docs/spec/fec-f3e/*` copied from OxFml-owned canonical seam specs for local implementation reference.

## Mirror policy
Foundation retains doctrine and conformance-policy ownership and keeps read-only mirrors.
