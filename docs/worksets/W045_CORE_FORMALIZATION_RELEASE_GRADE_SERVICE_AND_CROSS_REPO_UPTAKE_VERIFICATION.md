# W045 Core Formalization Release-Grade Service And Cross-Repo Uptake Verification

Status: `planned_successor_scope`

Parent predecessor: `W044`
Parent epic: `calc-zkio`

## 1. Purpose

Continue after W044 by converting remaining release-grade no-promotion blockers into direct implementation, proof/model, operated-service, independent-evaluator, OxFml public-surface, W073 downstream-uptake, continuous-scale, pack-governance, and release-grade evidence where possible, while preserving exact blockers where direct evidence is still insufficient.

W045 treats the current OxFml formatting and public-surface updates as active intake:

1. W073 aggregate and visualization conditional-formatting metadata is `typed_rule`-only.
2. W072 bounded `thresholds` strings are intentionally ignored for `colorScale`, `dataBar`, `iconSet`, `top`, `bottom`, `aboveAverage`, and `belowAverage`.
3. DNA OneCalc typed-rule request construction remains required downstream uptake and is not verified by OxCalc.
4. Ordinary OxFml downstream use should target `consumer::runtime`, `consumer::editor`, and `consumer::replay`; public `substrate::...` access is not an ordinary integration contract.

## 2. Scope

In scope:

1. Map W044 no-promotion blockers to W045 obligations with exact owner beads and promotion consequences.
2. Deepen optimized/core dynamic-transition, snapshot/capability counterpart, and callable metadata projection evidence.
3. Deepen Rust totality/refinement and panic-surface proof evidence.
4. Deepen Lean/TLA verification, scheduler fairness, unbounded-model, and totality-boundary evidence.
5. Widen Stage 2 production partition-analyzer soundness, scheduler equivalence, observable invariance, and pack-grade equivalence evidence.
6. Move operated assurance, retained-history, retained-witness lifecycle, retention SLO, alert/quarantine, and cross-engine service claims beyond file-backed envelopes where feasible.
7. Widen independent evaluator implementation authority, mismatch quarantine behavior, retained-witness attachment, and operated differential evidence.
8. Verify current OxFml public consumer surface, W073 downstream typed-rule request construction uptake where possible, `format_delta`/`display_delta` boundaries, `LET`/`LAMBDA` callable carrier, callable metadata/carrier sufficiency, registered-external projection, and provider-failure/callable-publication watch lanes.
9. Convert W044 release-scale measurements into stronger continuous semantic-assurance evidence where feasible, without treating timing data as correctness proof.
10. Reassess pack-grade replay governance service, `cap.C5.pack_valid`, and release-grade verification from direct evidence only.

Out of scope:

1. Editing OxFml, OxFunc, DNA OneCalc, or host repos directly from this repo.
2. Claiming broad OxFml, public migration, or downstream W073 uptake without direct current-surface evidence.
3. Treating W073 old aggregate/visualization threshold strings as fallback metadata.
4. Promoting release-grade, C5, pack-grade replay, Stage 2, operated service, independent evaluator, mismatch quarantine, scaling correctness, callable, registered-external, provider-publication, or general OxFunc claims from proxy evidence.

## 3. Exit Gate

W045 exits only when:

1. W044 residual lanes are mapped to W045 obligations, evidence packets, exact blockers, or successor scope.
2. Current OxFml inbound observations are reviewed, including W073 typed-rule direct replacement and public consumer surface updates.
3. Optimized/core, Rust, Lean/TLA, Stage 2, operated-service, independent-evaluator, OxFml/callable, release-scale, pack/C5, and release-grade lanes emit direct evidence or exact blockers.
4. Pack/C5 and release-grade decisions state exact evidence consequence before any promotion claim.
5. Closure audit includes prompt-to-artifact checklist, OPERATIONS checklist, completion-claim self-audit, semantic-equivalence statement, direct-evidence coverage audit, reviewed inbound observations line, and three-axis report.

## 4. Planned Bead Path

1. `calc-zkio.1` - W045 residual release-grade successor obligation and current OxFml intake map.
2. `calc-zkio.2` - W045 optimized core counterpart coverage and callable metadata projection closure.
3. `calc-zkio.3` - W045 Rust totality refinement and panic-surface hardening.
4. `calc-zkio.4` - W045 Lean TLA verification fairness and totality discharge.
5. `calc-zkio.5` - W045 Stage 2 production partition and pack-grade equivalence service evidence.
6. `calc-zkio.6` - W045 operated assurance retained-history retained-witness SLO service implementation.
7. `calc-zkio.7` - W045 independent evaluator breadth mismatch quarantine and operated differential service.
8. `calc-zkio.8` - W045 OxFml public surface W073 downstream typed formatting callable and registered-external uptake.
9. `calc-zkio.9` - W045 continuous release-scale assurance and semantic regression service.
10. `calc-zkio.10` - W045 pack-grade replay governance service and C5 reassessment.
11. `calc-zkio.11` - W045 closure audit and release-grade verification decision.

## 5. Evidence Policy

The W045 canonical artifact root is `docs/spec/core-engine/w045-formalization/` for packet docs and `docs/test-runs/core-engine/` for emitted evidence roots. Artifact roots remain checked in when they are normative baseline evidence for a bead.

Validation requirements scale by bead:

1. Code-bearing beads require focused tests plus relevant workspace checks.
2. Runner/evidence beads require deterministic artifact emission, JSON validation, and non-mutation of prior baseline artifacts.
3. Closure-audit beads require focused retests of the highest-risk seam and pack lanes, workset/bead validation, cycle checks, JSON validation, and diff hygiene.

## 6. Current Status

- execution_state: `calc-zkio.2_ready_optimized_core_counterpart_coverage_and_callable_metadata_projection_closure`
- scope_completeness: `scope_partial`
- target_completeness: `target_partial`
- integration_completeness: `partial`
- open_lanes:
  - `calc-zkio.1` residual successor obligation and current OxFml intake map is validated
  - `calc-zkio.2` optimized core counterpart coverage and callable metadata projection closure is ready next
  - release-grade verification remains unpromoted
  - pack-grade replay and C5 remain unpromoted
  - operated services, Stage 2 production policy, independent evaluator breadth, broad OxFml/public migration, callable metadata, W073 downstream uptake, continuous scale assurance, and general OxFunc kernels remain open or external as classified
