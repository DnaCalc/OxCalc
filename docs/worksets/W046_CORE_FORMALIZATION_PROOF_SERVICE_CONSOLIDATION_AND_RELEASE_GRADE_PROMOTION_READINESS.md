# W046 Core Formalization Proof-Service Consolidation And Release-Grade Promotion Readiness

Status: `planned_successor_scope`

Parent predecessor: `W045`

Parent epic: `calc-gucd`

## 1. Purpose

Continue after W045 by converting remaining release-grade no-promotion blockers into proof-service consolidation, direct optimized/core implementation, formal proof/model discharge, operated service evidence, independent evaluator authority, OxFml downstream uptake, continuous scale assurance, pack-grade replay governance, C5, and release-grade promotion-readiness evidence where feasible.

W046 is not a release-grade promotion by intent. It is the next direct-evidence tranche after W045’s closure audit. It keeps exact blockers where direct evidence remains insufficient and allows specs/scope to evolve from new evidence rather than treating earlier specs or implementations as immutable targets.

W046 carries the current OxFml formatting and public-surface intake:

1. W073 aggregate and visualization conditional-formatting metadata is `typed_rule`-only.
2. W072 bounded `thresholds` strings are intentionally ignored for `colorScale`, `dataBar`, `iconSet`, `top`, `bottom`, `aboveAverage`, and `belowAverage`.
3. DNA OneCalc typed-rule request construction remains required downstream uptake unless W046 obtains direct current evidence.
4. Ordinary OxFml downstream use should target `consumer::runtime`, `consumer::editor`, and `consumer::replay`; public `substrate::...` access is not an ordinary integration contract.

## 2. Scope

In scope:

1. Map W045 no-promotion blockers to W046 promotion-readiness obligations with exact owner beads and promotion consequences.
2. Attack optimized/core dynamic-transition breadth, snapshot/capability counterpart breadth, and callable metadata projection with direct implementation or retained blockers.
3. Deepen Rust totality/refinement, panic-surface, soft-reference, snapshot/capability, and callable-boundary proof evidence.
4. Deepen Lean/TLA proof/model evidence, scheduler fairness, unbounded-model boundaries, and totality dependencies.
5. Convert declared-profile Stage 2 scheduler and pack-equivalence evidence into production-policy readiness where direct evidence exists.
6. Push operated assurance, retained-history, retained-witness lifecycle, retention SLO, alert/quarantine, and cross-engine service evidence toward operated service readiness.
7. Widen independent evaluator implementation authority, mismatch quarantine behavior, retained-witness attachment, and operated differential evidence.
8. Verify current OxFml public surface, DNA OneCalc W073 downstream uptake where available, callable carrier/metadata, registered-external projection, and provider/callable publication lanes.
9. Convert W045 semantic regression and scale timing evidence into stronger operated continuous scale assurance where feasible while preserving performance-not-correctness guards.
10. Reassess pack-grade replay governance service, `cap.C5.pack_valid`, and release-grade verification from direct evidence only.

Out of scope:

1. Editing OxFml, OxFunc, DNA OneCalc, or host repos directly from this repo.
2. Claiming broad OxFml, public migration, or downstream W073 uptake without direct current-surface evidence.
3. Treating W073 old aggregate/visualization threshold strings as fallback metadata.
4. Promoting release-grade, full formalization, C5, pack-grade replay, Stage 2, operated service, independent evaluator, mismatch quarantine, scaling correctness, callable, registered-external, provider-publication, continuous scale assurance, or general OxFunc claims from proxy evidence.

## 3. Exit Gate

W046 exits only when:

1. W045 residual lanes are mapped to W046 obligations, evidence packets, exact blockers, or successor scope.
2. Current OxFml inbound observations are reviewed, including W073 typed-rule direct replacement and public consumer surface updates.
3. Optimized/core, Rust, Lean/TLA, Stage 2, operated-service, independent-evaluator, OxFml/callable, release-scale, pack/C5, and release-grade lanes emit direct evidence or exact blockers.
4. Pack/C5 and release-grade decisions state exact evidence consequence before any promotion claim.
5. Closure audit includes prompt-to-artifact checklist, OPERATIONS checklist, completion-claim self-audit, semantic-equivalence statement, direct-evidence coverage audit, reviewed inbound observations line, and three-axis report.

## 4. Planned Bead Path

1. `calc-gucd.1` - W046 residual promotion-readiness ledger and current uptake map.
2. `calc-gucd.2` - W046 optimized core broad transition snapshot capability and callable metadata implementation.
3. `calc-gucd.3` - W046 Rust totality refinement and panic-free core proof-service consolidation.
4. `calc-gucd.4` - W046 Lean TLA full-verification fairness and model-bound discharge consolidation.
5. `calc-gucd.5` - W046 Stage 2 production policy pack-equivalence and scheduler service promotion readiness.
6. `calc-gucd.6` - W046 operated assurance retained-history retained-witness retention-SLO alert service readiness.
7. `calc-gucd.7` - W046 independent evaluator mismatch quarantine retained-witness attachment operated differential readiness.
8. `calc-gucd.8` - W046 OxFml W073 downstream uptake public migration callable registered-external provider closure.
9. `calc-gucd.9` - W046 continuous scale assurance operated semantic regression and release-scale correctness guard.
10. `calc-gucd.10` - W046 pack-grade replay governance service and C5 promotion readiness decision.
11. `calc-gucd.11` - W046 closure audit and release-grade verification decision.

## 5. Evidence Policy

The W046 canonical artifact root is `docs/spec/core-engine/w046-formalization/` for packet docs and `docs/test-runs/core-engine/` for emitted evidence roots. Artifact roots remain checked in when they are normative baseline evidence for a bead.

Validation requirements scale by bead:

1. Code-bearing beads require focused tests plus relevant workspace checks.
2. Runner/evidence beads require deterministic artifact emission, JSON validation, and non-mutation of prior baseline artifacts.
3. Closure-audit beads require focused retests of the highest-risk seam and pack lanes, workset/bead validation, cycle checks, JSON validation, and diff hygiene.

## 6. Current Status

- execution_state: `calc-gucd.1_ready_after_w045_closure`
- scope_completeness: `scope_partial`
- target_completeness: `target_partial`
- integration_completeness: `partial`
- open_lanes:
  - `calc-gucd.1` residual promotion-readiness ledger and current uptake map is next
  - release-grade verification remains unpromoted
  - pack-grade replay and C5 remain unpromoted
  - operated services, Stage 2 production policy, independent evaluator breadth, broad OxFml/public migration, callable metadata, W073 downstream uptake, continuous scale assurance, and general OxFunc kernels remain open or external as classified
